# افسانه زال | Legend of Zal

یک بازی پلتفرمر-ماجراجویی برگرفته از داستان **زال، فرزند سیمرغ** در شاهنامه فردوسی.
این مخزن شامل **دموی فنی قابل بازی (مرحله ۱)** و **اسکلت کامل پروژه** برای هر ۸ مرحله فصل اول است.

> هدف این نسخه: ساخت یک دموی قابل ارائه به سرمایه‌گذار، و سپس تکمیل بازی کامل.

---

## ۱. اجرای دمو

نیازی به نصب چیزی نیست. فقط فایل `index.html` را در مرورگر باز کن.

برای تجربه بهتر (و جلوگیری از محدودیت‌های مرورگر) می‌توانی یک سرور محلی ساده اجرا کنی:

```bash
# با پایتون
python3 -m http.server 8000
# سپس باز کن: http://localhost:8000
```

روی موبایل: همان آدرس را در مرورگر گوشی باز کن؛ کنترل‌های لمسی به‌صورت خودکار ظاهر می‌شوند.

## ۲. کنترل‌ها

| عمل | کیبورد | لمسی |
|-----|--------|------|
| حرکت | کلیدهای جهت / `A` `D` | دکمه‌های ◀ ▶ |
| پرش | `Space` / `W` / ↑ | دکمه «پرش» |
| عقاب (از مرحله ۳) | `E` | دکمه «عقاب» |
| تأیید / بازگشت | `Enter` / `Esc` | لمس روی صفحه |

**هدف مرحله ۱:** بالا رفتن از کوه البرز، جمع‌آوری میوه و آب، فرار/مقابله با گرگ‌ها و گربه کوهی،
شکست **گرگ پیر کوهستان** (با پریدن روی سرش)، و رسیدن به **سکوی مقدس سیمرغ**.

## ۳. ساختار پروژه

```
.
├── index.html              # نقطه ورود
├── css/style.css           # استایل و کنترل‌های لمسی
├── js/
│   ├── utils.js            # توابع کمکی + داده هر ۸ مرحله (STAGES)
│   ├── input.js            # ورودی کیبورد/لمس/ماوس
│   ├── audio.js            # افکت‌های صوتی WebAudio
│   ├── scene.js            # کلاس پایه صحنه + مدیر صحنه + دکمه UI
│   ├── game.js             # حلقه اصلی و راه‌اندازی
│   ├── entities/
│   │   ├── player.js       # زال (فیزیک، جان، انیمیشن)
│   │   ├── enemy.js        # گرگ، گربه کوهی، باس
│   │   ├── item.js         # میوه و آب
│   │   └── eagle.js        # عقاب همراه (مکانیک امضای بازی)
│   └── scenes/
│       ├── menu.js         # منوی اصلی
│       ├── about.js        # صفحه داستان و راهنما
│       ├── stageSelect.js  # انتخاب مرحله (نمای ۸ مرحله)
│       ├── stage1.js       # مرحله ۱ — پلتفرمر (قابل بازی)
│       ├── stage2.js       # مرحله ۲ — پرواز سیمرغ (قابل بازی)
│       ├── stage3.js       # مرحله ۳ — مکانیک عقاب (قابل بازی)
│       ├── platformerStage.js # کلاس پایهٔ مشترک پلتفرمر-مبارزه
│       ├── stage4.js … stage7.js # مراحل ۴ تا ۷ (روی کلاس پایه)
│       ├── stage8.js       # مرحله ۸ — فرار سوارهٔ زال
│       └── placeholder.js  # نگه‌دارنده (دیگر استفاده نمی‌شود)
├── resources/              # آیکون و splash منبع (پر سیمرغ)
├── scripts/
│   ├── build-www.js        # ساخت پوشه www/ برای Capacitor
│   └── gen-assets.py       # تولید آیکون و splash
├── capacitor.config.json   # پیکربندی اپ اندروید
├── package.json            # اسکریپت‌های ساخت اندروید
├── .github/workflows/      # انتشار خودکار روی GitHub Pages
└── docs/
    ├── GDD.md              # سند طراحی بازی
    └── ANDROID.md          # راهنمای ساخت APK
```

## ۴. مکانیک امضای بازی: عقاب همراه

از مرحله ۳ به بعد، عقاب ایرانی همراه زال می‌شود. با نگه‌داشتن دکمه عقاب،
عقاب یک نگهبان/دشمن را برای **مدت محدودی** سرگرم می‌کند تا تو فرصت کنی بقیه را از پای درآوری.
اگر دیر کنی، عقاب خسته می‌شود، شکست می‌خورد و به زمین می‌افتد (زمان انتظار افزایش می‌یابد).
این منطق در `js/entities/eagle.js` پیاده‌سازی شده و آماده استفاده در مراحل بعدی است.

## ۴.۱ صدا، موسیقی و روایت 🎵

تمام صدای بازی **به‌صورت برنامه‌ای و زنده سنتز می‌شود** (WebAudio) و هیچ فایل صوتی بیرونی
ندارد — یعنی **کاملاً بدون مشکل کپی‌رایت**:

- **موسیقی پس‌زمینه** (`js/music.js`): سنتز زخمه‌ای شبیه **تار/سنتور** روی گام **دستگاه شور**
  (با ربع‌پردهٔ ایرانی) + ضرب **تنبک** و فضای آکوستیک. پس از اولین تعامل کاربر آغاز می‌شود.
- **روایت داستان با هوش مصنوعی** (`js/narrator.js`): داستان اول و آخر هر مرحله با **صدای نورال
  فارسی** خوانده می‌شود. فایل‌های صوتی را یک‌بار با `npm run narration` بساز (راهنما:
  [`docs/AUDIO.md`](docs/AUDIO.md)). اگر فایل نبود، به صدای مرورگر برمی‌گردد.
- **افکت‌ها** (`js/audio.js`): پرش، برخورد، جمع‌آوری و… .

خاموش/روشن کردن موسیقی و روایت از صفحهٔ **«داستان و راهنما»**.

## ۵. نقشه راه مراحل

نگاه کن به [`docs/GDD.md`](docs/GDD.md) برای طرح کامل هر ۸ مرحله، گیم‌پلی و باس‌ها.

| # | مرحله | محیط | وضعیت |
|---|-------|------|-------|
| ۱ | کودک رهاشده | کوه البرز | ✅ قابل بازی (پلتفرمر) |
| ۲ | پرواز سیمرغ | آسمان البرز | ✅ قابل بازی (پرواز) |
| ۳ | نجات بچه عقاب | کوه البرز | ✅ قابل بازی (مکانیک عقاب 🦅) |
| ۴ | بازگشت به ایران | دربار شاه | ✅ قابل بازی (دوئل پهلوان) |
| ۵ | کفتار دشت زابل | زابلستان | ✅ قابل بازی (شکار کفتار) |
| ۶ | عشق رودابه | شهر کابل | ✅ قابل بازی (نفوذ و صعود) |
| ۷ | فرمانروای زابل | نقشه جهان | ✅ قابل بازی (نبرد با دیو) |
| ۸ | تولد رستم | پر سیمرغ | ✅ قابل بازی (فرار سواره) |

🎉 **کل فصل اول (هر ۸ مرحله) اکنون قابل بازی است.**

## ۶. تبدیل به اپ اندروید (APK)

این پروژه با **Capacitor** پیکربندی شده تا همین فایل‌های وب به یک اپ بومی اندروید تبدیل شوند:

```bash
npm install
npm run android:add     # فقط بار اول
npm run android:open    # باز کردن در Android Studio برای ساخت APK
```

راهنمای کامل: [`docs/ANDROID.md`](docs/ANDROID.md).

> در صورت نیاز به گرافیک سنگین‌تر در آینده، می‌توان منطق بازی را به **Godot** منتقل کرد؛
> داده مراحل (`STAGES`) و سند GDD مستقل از موتورند و قابل انتقال‌اند.

## ۶.۱ انتشار آنلاین (GitHub Pages)

با هر push، ورک‌فلوی [`.github/workflows/pages.yml`](.github/workflows/pages.yml) دمو را
روی GitHub Pages منتشر می‌کند. برای فعال‌سازی (فقط یک‌بار):

1. در مخزن گیت‌هاب: **Settings → Pages**
2. در بخش **Build and deployment**، گزینه **Source** را روی **GitHub Actions** بگذار.

پس از آن، لینک عمومی دمو در تب **Actions** (یا Settings → Pages) نمایش داده می‌شود و می‌توانی
آن را مستقیماً به سرمایه‌گذار بدهی.

## ۷. توسعه بعدی (پیشنهادی)

1. جایگزینی گرافیک برداری فعلی با اسپرایت‌های نقاشی‌شده به سبک مینیاتور ایرانی.
2. افزودن موسیقی و صداگذاری.
3. ساخت مرحله ۲ (پرواز سیمرغ) به‌عنوان مرحله دوم قابل بازی.
4. سیستم ذخیره پیشرفت (localStorage).
